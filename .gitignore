# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# .terraform.lock.hcl — intentionally tracked for reproducible builds

# Lambda build artifacts
**/.build/

# tflint plugin cache
.tflint.d/

# OS
.DS_Store
